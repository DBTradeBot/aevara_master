import 'package:flutter/material.dart'; class ExperimentCard extends StatelessWidget{final String title; final String summary; final VoidCallback? onTap; const ExperimentCard({super.key, required this.title, required this.summary, this.onTap}); @override Widget build(BuildContext c)=>Card(child: ListTile(title: Text(title), subtitle: Text(summary), trailing: const Icon(Icons.chevron_right), onTap: onTap));}
