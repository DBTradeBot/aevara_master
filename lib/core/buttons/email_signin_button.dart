import 'package:flutter/material.dart'; class EmailSignInButton extends StatelessWidget{final VoidCallback? onPressed; final String label; const EmailSignInButton({super.key, this.onPressed, this.label='Continue with Email'}); @override Widget build(BuildContext c)=>OutlinedButton.icon(onPressed:onPressed, icon: const Icon(Icons.mail_outline), label: Text(label));}
