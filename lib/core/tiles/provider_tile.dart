import 'package:flutter/material.dart'; class ProviderTile extends StatelessWidget{final String name; final String status; final VoidCallback? onTap; const ProviderTile({super.key, required this.name, required this.status, this.onTap}); @override Widget build(BuildContext c)=>ListTile(leading: const Icon(Icons.watch), title: Text(name), subtitle: Text(status), trailing: const Icon(Icons.chevron_right), onTap: onTap);}
