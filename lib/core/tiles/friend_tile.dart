import 'package:flutter/material.dart'; class FriendTile extends StatelessWidget{final String name; final VoidCallback? onTap; const FriendTile({super.key, required this.name, this.onTap}); @override Widget build(BuildContext c)=>ListTile(leading: const CircleAvatar(child: Icon(Icons.person)), title: Text(name), onTap: onTap);}
