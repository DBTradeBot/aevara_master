import 'package:flutter/material.dart';

class DataHubPage extends StatelessWidget {
  const DataHubPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Data Hub placeholder'),
    );
  }
}
