// functions/index.js
// ESM barrel for Firebase Functions (Node 20 / Gen2)

/* ------------------------------ DDC endpoints ------------------------------ */
export { computeDailyHttp } from "./ddc/compute_http.js";
export { computeRangeCall, computeRangeHttp } from "./ddc/compute_range.js";
export { finalizeDailyDDC } from "./ddc/finalize.js";

/* --------------------------- Compute wrappers ------------------------------ */
export { callComputeDaily, computeDailyProxy } from "./call_computeDaily.js";

/* ------------------------------ Baseline flows ----------------------------- */
export {
  ensureBaselineOnLogin,
  ensureBaselineOnLoginHttp,
  lightRefresh4,
} from "./baseline/index.js";

/* ------------------------------- Notifications ----------------------------- */
export * from "./notifications.js";

/* ----------------------------- Community (all) ------------------------------ */
export * from "./community/index.js";

/* ----------------------------- Normalization triggers ---------------------- */
export {
  normMeasurements,
  normWorkouts,
  normHydration,
  normGlucose,
  normTemp,
  normBloodPressure,
  enrichBP, // NOTE: now a background trigger, matching the previously deployed type
} from "./normalize/index.js";

/* ------------------------------- Vendor / OAuth ---------------------------- */
export * from "./fitbit_fetch.js";
export * from "./fitbit_oauth.js";

/* ------------------------------- Integrations ------------------------------ */
export { disconnectProviderCall } from "./disconnectProvider.js";
export { setIntegrationStatus } from "./setIntegrationStatus.js";

/* --------------------------------- Seeders --------------------------------- */
export { seedFirestore } from "./seed/index.js";
